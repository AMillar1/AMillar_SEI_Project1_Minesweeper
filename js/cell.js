// Board object and each of the cells. 
class Cell{
   constructor(isMined, isShown, isFlagged, adjSum){
       this.isMined = ismined; //should be a bool
       this.isShown = isShown; //also a bool
       this.isFlagged = isFlagged; //yet another bool
       this.adjSum = foo; //This one will be quite a bit of code
   } 
   show() {
       this.isShown = true;
   }
   calcAdj() {
       
   }
}